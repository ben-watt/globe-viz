apiVersion: v1
kind: Service
metadata:
  name: {{ include "shipments-viz.fullname" . }}
  labels:
    {{- include "shipments-viz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shipments-viz.selectorLabels" . | nindent 4 }}
